Thấy 2 vấn đề:
Bug 1 — line 396: fallback sai
jsconst chance = masterInfo.chance || 1.0;
Dragon có chance: 0.0 → falsy → fallback về 1.0 — Dragon hiển thị 1% thay vì 0% 😄
Fix:
jsconst chance = masterInfo.chance != null ? masterInfo.chance : null;
Bug 2 — line 412: Dragon hiển thị "0%" hay "Off-sale"?
Cần xử lý riêng:
jsconst chanceDisplay = chance === 0 ? 'Off-sale' : chance === null ? 'N/A' : `${chance}%`;